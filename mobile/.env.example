# Public HTTPS API. Never 127.0.0.1 on a physical phone (that is the phone itself).
# LAN debug: http://192.168.x.x:8000 with CORS_ORIGIN_REGEX on the API.
EXPO_PUBLIC_API_BASE=https://api.example.com
