# Origin of the standalone Rituchakra API (no trailing slash).
# The Next.js app does not ship API logic. Leave unset to use http://127.0.0.1:8000
# Set empty to use same-origin /api plus the optional Next rewrite.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
